# Workflows

Use a tight validation loop while changing product code:

1. Edit the relevant app, package, or Convex surface.
2. Run `pnpm run validate`.
3. Inspect behavior with framework observability commands when events, jobs, or cloud state are involved.
4. Re-run validation before handing work off.
