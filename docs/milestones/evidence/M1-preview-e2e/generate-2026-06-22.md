# M1 Generate Evidence — 2026-06-22

## Scope

M1 **Generate** checkbox only. No provider resources were created, linked, adopted, mutated, or inspected live.

## App

- Path: `tmp/m1-preview-generate`
- Template: local `create-agent-stack` B2B SaaS template
- CLI: source checkout under `<agentstack-repo>`

## Commands

```bash
cd <agentstack-repo>/tmp
<agentstack-repo>/node_modules/.bin/tsx \
  <agentstack-repo>/packages/create-agent-stack/src/bin.ts \
  m1-preview-generate
```

Output:

```text
Created m1-preview-generate
```

```bash
AGENTSTACK_CLI_BIN=<agentstack-repo>/packages/cli/src/bin.ts \
AGENTSTACK_TSX_BIN=<agentstack-repo>/node_modules/.bin/tsx \
pnpm --dir <agentstack-repo>/tmp/m1-preview-generate run validate
```

Output:

```text
Evidence: local-structure
Scope: local filesystem structure only; no package commands
PASS validate
```

```bash
AGENTSTACK_CLI_BIN=<agentstack-repo>/packages/cli/src/bin.ts \
AGENTSTACK_TSX_BIN=<agentstack-repo>/node_modules/.bin/tsx \
pnpm --dir <agentstack-repo>/tmp/m1-preview-generate run generated:check
```

Output:

```text
Evidence: generated-boundary
Scope: generated anchors and guidance only; no local-cloud writes; no provider executor; no live provider reads
Required generated anchors: 56
Missing generated anchors: 0
Guidance anchors: 5
Expected guidance version: 2026-06-20
PASS generated validate
```

The generated `docs/agentstack/preview.md` also includes an M1 web-only path that directs the operator to the Clerk, Convex, and Vercel per-service provider plans and explicitly keeps EAS out of M1 unless mobile scope is unlocked.

## Runtime Build Probe

The regenerated app was installed under `tmp/m1-preview-generate` with lifecycle scripts disabled, then the web build was run:

```bash
pnpm --dir <agentstack-repo>/tmp/m1-preview-generate install --ignore-scripts
pnpm --dir <agentstack-repo>/tmp/m1-preview-generate --filter @app/web build
```

Key output:

```text
@app/web@ build
vite build
✓ built
```

The generated web app now contains Clerk provider wiring, ConvexProviderWithClerk, Vite env names, and a `workspaceStatus.protectedStatus` Convex query that checks `ctx.auth.getUserIdentity()`. This supports the later M1 Auth/Data smoke, but the Auth/Data checkboxes remain unchecked until the app is deployed to a preview URL and exercised with real provider resources.

## Provider Plan Probe

These commands are local plan-only probes. They did not read live providers, write local-cloud state, mutate provider resources, or update the provider ledger.

```bash
AGENTSTACK_CLI_BIN=<agentstack-repo>/packages/cli/src/bin.ts \
AGENTSTACK_TSX_BIN=<agentstack-repo>/node_modules/.bin/tsx \
pnpm --dir <agentstack-repo>/tmp/m1-preview-generate run provider:clerk:preview
```

Key output:

```text
PLAN provider clerk preview
Evidence: provider-command-plan
Provider execution: none
Resource: application m1-preview-generate-preview
Ledger: missing
Lifecycle: create
Lifecycle reason: ledger-missing
```

```bash
AGENTSTACK_CLI_BIN=<agentstack-repo>/packages/cli/src/bin.ts \
AGENTSTACK_TSX_BIN=<agentstack-repo>/node_modules/.bin/tsx \
pnpm --dir <agentstack-repo>/tmp/m1-preview-generate run provider:convex:preview
```

Key output:

```text
PLAN provider convex preview
Evidence: provider-command-plan
Provider execution: none
Resource: deployment m1-preview-generate-preview
Ledger: missing
Lifecycle: create
Lifecycle reason: ledger-missing
Required env: CONVEX_DEPLOY_KEY
```

```bash
AGENTSTACK_CLI_BIN=<agentstack-repo>/packages/cli/src/bin.ts \
AGENTSTACK_TSX_BIN=<agentstack-repo>/node_modules/.bin/tsx \
pnpm --dir <agentstack-repo>/tmp/m1-preview-generate run provider:vercel:preview
```

Key output:

```text
PLAN provider vercel preview
Evidence: provider-command-plan
Provider execution: none
Resource: project m1-preview-generate
Ledger: missing
Lifecycle: create
Lifecycle reason: ledger-missing
Required env: VERCEL_TOKEN
```

## Result

PASS for M1 Generate. The app was created from the local B2B SaaS template, the generated anchor gate reported zero missing anchors, and the generated web app built after installing dependencies. The next M1 checkbox remains blocked by missing preview provider ledger rows, not by generation, web runtime wiring, or provider command planning.

## Next Smallest Step

Record preview Clerk, Convex, and Vercel resource rows in `docs/provider-resource-ledger.md` before any real provider create/link/adopt/mutate action.
