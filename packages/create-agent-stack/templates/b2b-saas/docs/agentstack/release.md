# Release

Plan production changes with framework release commands, then apply them through the same framework path.

Run release validation before production apply so environment sync, provider readiness, and generated boundaries are checked together.
